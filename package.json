{
  "name": "vite-mono-react",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "clean": "pnpm -r --stream exec rimraf dist node_modules && rimraf dist node_modules",
    "dev": "pnpm -F myapp dev",
    "build": "pnpm -F myapp",
    "preview": "pnpm -F myapp preview"
  },
  "devDependencies": {
    "esbuild": "^0.14.53",
    "rimraf": "^3.0.2",
    "typescript": "^4.6.4",
    "vite": "^3.0.0"
  }
}